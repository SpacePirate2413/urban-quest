# Testing

Auto-generated by `/testing` (see `scripts/update-testing-doc.mjs`). Do not edit by hand.

_Last run: 2026-05-03T20:40:33.635Z_

## Summary

- ✅ **67 passing**
- ❌ **0 failing**
- **67 total**

### ✅ Passing tests

- ✅ [#1](#test-1) — isQuestPlayable returns true for the minimal valid shape (one waypoint, one scene, END choice)
- ✅ [#2](#test-2) — isQuestPlayable rejects a quest with no waypoints
- ✅ [#3](#test-3) — isQuestPlayable rejects a quest with no scenes
- ✅ [#4](#test-4) — isQuestPlayable rejects a scene with an empty question
- ✅ [#5](#test-5) — isQuestPlayable rejects a scene with a whitespace-only question
- ✅ [#6](#test-6) — isQuestPlayable rejects a scene with a null question
- ✅ [#7](#test-7) — isQuestPlayable rejects a scene with no mediaUrl
- ✅ [#8](#test-8) — isQuestPlayable rejects a scene whose choices field is not valid JSON
- ✅ [#9](#test-9) — isQuestPlayable rejects a scene whose choices array is empty
- ✅ [#10](#test-10) — isQuestPlayable rejects a scene whose choices field parses to a non-array
- ✅ [#11](#test-11) — isQuestPlayable rejects a choice with empty text
- ✅ [#12](#test-12) — isQuestPlayable rejects a choice pointing at a sceneId that does not exist on the quest
- ✅ [#13](#test-13) — isQuestPlayable accepts a choice pointing at a valid sceneId on the quest
- ✅ [#14](#test-14) — isQuestPlayable accepts a legacy choice pointing at a valid waypointId (back-compat)
- ✅ [#15](#test-15) — isQuestPlayable rejects a legacy choice pointing at a waypointId that does not exist
- ✅ [#16](#test-16) — isQuestPlayable rejects a choice with neither sceneId nor waypointId set
- ✅ [#17](#test-17) — isQuestPlayable accepts the END sentinel as a routing target without needing a real id
- ✅ [#18](#test-18) — Admin: submissions feed lists quests with a submissionStatus
- ✅ [#19](#test-19) — Admin: submissions feed omits quests with no submissionStatus (pure drafts)
- ✅ [#20](#test-20) — Admin: submissions feed filters by submissionStatus query param
- ✅ [#21](#test-21) — Admin: quest-level review approves the quest and cascades approved status + notes to every scene
- ✅ [#22](#test-22) — Admin: quest-level review rejects the quest and stamps the rejection note on every scene
- ✅ [#23](#test-23) — Admin: quest-level review returns 404 when reviewing a non-existent quest
- ✅ [#24](#test-24) — Admin: quest-level review rejects a payload with an invalid status enum
- ✅ [#25](#test-25) — Admin: scene order in submissions feed returns scenes in addScene insertion order, with sceneIndex 0..N matching it
- ✅ [#26](#test-26) — API Application Health Check should return health status
- ✅ [#27](#test-27) — API Application App Bootstrap should have CORS configured
- ✅ [#28](#test-28) — API Application App Bootstrap should have JWT configured
- ✅ [#29](#test-29) — API Application App Bootstrap should have cookie support
- ✅ [#30](#test-30) — API Application Users Routes should have users routes registered
- ✅ [#31](#test-31) — Auth: protected routes rejects /api/users/me with no token
- ✅ [#32](#test-32) — Auth: protected routes returns the user for /api/users/me with a valid token
- ✅ [#33](#test-33) — Auth: protected routes rejects /api/users/me with a malformed token
- ✅ [#34](#test-34) — Quest publish + submit-for-review refuses to publish a quest with no waypoints
- ✅ [#35](#test-35) — Quest publish + submit-for-review refuses to publish a quest with no scenes
- ✅ [#36](#test-36) — Quest publish + submit-for-review publishes a quest that has a waypoint and a scene
- ✅ [#37](#test-37) — Quest publish + submit-for-review refuses submit-for-review when any scene is missing media
- ✅ [#38](#test-38) — Quest publish + submit-for-review marks the quest pending when all scenes have media
- ✅ [#39](#test-39) — Edit-published-quest re-review trigger flags submissionStatus=needs_re_review when a published quest title changes
- ✅ [#40](#test-40) — Edit-published-quest re-review trigger does not flag re-review when the changed field is non-content (e.g. coordinates)
- ✅ [#41](#test-41) — Edit-published-quest re-review trigger does not re-flag a quest already pending review
- ✅ [#42](#test-42) — Quests CRUD creates a quest with the authenticated user as author
- ✅ [#43](#test-43) — Quests CRUD rejects quest creation without auth
- ✅ [#44](#test-44) — Quests CRUD rejects quest creation with empty title
- ✅ [#45](#test-45) — Quests CRUD returns only my quests from /api/quests/my
- ✅ [#46](#test-46) — Quests CRUD updates a quest the user owns
- ✅ [#47](#test-47) — Quests CRUD refuses to update a quest the user does not own
- ✅ [#48](#test-48) — Quests CRUD deletes a quest the user owns
- ✅ [#49](#test-49) — Quests CRUD accepts ageRating E10+ (the value the creator-station UI offers)
- ✅ [#50](#test-50) — Quests CRUD accepts mediaType=both for mixed-format quests
- ✅ [#51](#test-51) — Quests CRUD rejects an unknown mediaType value
- ✅ [#52](#test-52) — Scene CRUD creates a scene under a quest the user owns
- ✅ [#53](#test-53) — Scene CRUD refuses to create a scene under a quest the user does not own
- ✅ [#54](#test-54) — Scene CRUD updates and deletes a scene
- ✅ [#55](#test-55) — Scene media upload rejects upload to an unknown sceneId with 404 (not "Load failed")
- ✅ [#56](#test-56) — Scene media upload rejects upload of a disallowed mime type
- ✅ [#57](#test-57) — Scene media upload accepts an audio upload and attaches mediaUrl to the scene
- ✅ [#58](#test-58) — Scouted Waypoints — edit flow PATCHes name + notes and persists across reads
- ✅ [#59](#test-59) — Scouted Waypoints — edit flow PATCHes lat/lng (move-pin flow)
- ✅ [#60](#test-60) — Scouted Waypoints — edit flow rejects PATCH from a different user with 404
- ✅ [#61](#test-61) — Scouted Waypoints — edit flow rejects an empty name (validation)
- ✅ [#62](#test-62) — Scouted Waypoints — edit flow rejects unauthenticated PATCH with 401
- ✅ [#63](#test-63) — Waypoint CRUD creates a waypoint under a quest the user owns
- ✅ [#64](#test-64) — Waypoint CRUD rejects creating a waypoint under another user's quest
- ✅ [#65](#test-65) — Waypoint CRUD PATCHes a waypoint name and the new name persists across reads
- ✅ [#66](#test-66) — Waypoint CRUD rejects PATCH from a non-owner with 404
- ✅ [#67](#test-67) — Waypoint CRUD deletes a waypoint the user owns

---

## Tests

### <a id="test-1"></a>✅ #1 — isQuestPlayable returns true for the minimal valid shape (one waypoint, one scene, END choice)

**Category:** Unit

**What it does:** returns true for the minimal valid shape (one waypoint, one scene, END choice)

**Test location:** [`apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts`](apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts)

### <a id="test-2"></a>✅ #2 — isQuestPlayable rejects a quest with no waypoints

**Category:** Unit

**What it does:** rejects a quest with no waypoints

**Test location:** [`apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts`](apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts)

### <a id="test-3"></a>✅ #3 — isQuestPlayable rejects a quest with no scenes

**Category:** Unit

**What it does:** rejects a quest with no scenes

**Test location:** [`apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts`](apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts)

### <a id="test-4"></a>✅ #4 — isQuestPlayable rejects a scene with an empty question

**Category:** Unit

**What it does:** rejects a scene with an empty question

**Test location:** [`apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts`](apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts)

### <a id="test-5"></a>✅ #5 — isQuestPlayable rejects a scene with a whitespace-only question

**Category:** Unit

**What it does:** rejects a scene with a whitespace-only question

**Test location:** [`apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts`](apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts)

### <a id="test-6"></a>✅ #6 — isQuestPlayable rejects a scene with a null question

**Category:** Unit

**What it does:** rejects a scene with a null question

**Test location:** [`apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts`](apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts)

### <a id="test-7"></a>✅ #7 — isQuestPlayable rejects a scene with no mediaUrl

**Category:** Unit

**What it does:** rejects a scene with no mediaUrl

**Test location:** [`apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts`](apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts)

### <a id="test-8"></a>✅ #8 — isQuestPlayable rejects a scene whose choices field is not valid JSON

**Category:** Unit

**What it does:** rejects a scene whose choices field is not valid JSON

**Test location:** [`apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts`](apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts)

### <a id="test-9"></a>✅ #9 — isQuestPlayable rejects a scene whose choices array is empty

**Category:** Unit

**What it does:** rejects a scene whose choices array is empty

**Test location:** [`apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts`](apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts)

### <a id="test-10"></a>✅ #10 — isQuestPlayable rejects a scene whose choices field parses to a non-array

**Category:** Unit

**What it does:** rejects a scene whose choices field parses to a non-array

**Test location:** [`apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts`](apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts)

### <a id="test-11"></a>✅ #11 — isQuestPlayable rejects a choice with empty text

**Category:** Unit

**What it does:** rejects a choice with empty text

**Test location:** [`apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts`](apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts)

### <a id="test-12"></a>✅ #12 — isQuestPlayable rejects a choice pointing at a sceneId that does not exist on the quest

**Category:** Unit

**What it does:** rejects a choice pointing at a sceneId that does not exist on the quest

**Test location:** [`apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts`](apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts)

### <a id="test-13"></a>✅ #13 — isQuestPlayable accepts a choice pointing at a valid sceneId on the quest

**Category:** Unit

**What it does:** accepts a choice pointing at a valid sceneId on the quest

**Test location:** [`apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts`](apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts)

### <a id="test-14"></a>✅ #14 — isQuestPlayable accepts a legacy choice pointing at a valid waypointId (back-compat)

**Category:** Unit

**What it does:** accepts a legacy choice pointing at a valid waypointId (back-compat)

**Test location:** [`apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts`](apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts)

### <a id="test-15"></a>✅ #15 — isQuestPlayable rejects a legacy choice pointing at a waypointId that does not exist

**Category:** Unit

**What it does:** rejects a legacy choice pointing at a waypointId that does not exist

**Test location:** [`apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts`](apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts)

### <a id="test-16"></a>✅ #16 — isQuestPlayable rejects a choice with neither sceneId nor waypointId set

**Category:** Unit

**What it does:** rejects a choice with neither sceneId nor waypointId set

**Test location:** [`apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts`](apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts)

### <a id="test-17"></a>✅ #17 — isQuestPlayable accepts the END sentinel as a routing target without needing a real id

**Category:** Unit

**What it does:** accepts the END sentinel as a routing target without needing a real id

**Test location:** [`apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts`](apps/api/src/features/quests/__tests__/isQuestPlayable.test.ts)

### <a id="test-18"></a>✅ #18 — Admin: submissions feed lists quests with a submissionStatus

**Category:** Integration

**What it does:** lists quests with a submissionStatus

**Test location:** [`apps/api/tests/admin.test.ts`](apps/api/tests/admin.test.ts)

### <a id="test-19"></a>✅ #19 — Admin: submissions feed omits quests with no submissionStatus (pure drafts)

**Category:** Integration

**What it does:** omits quests with no submissionStatus (pure drafts)

**Test location:** [`apps/api/tests/admin.test.ts`](apps/api/tests/admin.test.ts)

### <a id="test-20"></a>✅ #20 — Admin: submissions feed filters by submissionStatus query param

**Category:** Integration

**What it does:** filters by submissionStatus query param

**Test location:** [`apps/api/tests/admin.test.ts`](apps/api/tests/admin.test.ts)

### <a id="test-21"></a>✅ #21 — Admin: quest-level review approves the quest and cascades approved status + notes to every scene

**Category:** Integration

**What it does:** approves the quest and cascades approved status + notes to every scene

**Test location:** [`apps/api/tests/admin.test.ts`](apps/api/tests/admin.test.ts)

### <a id="test-22"></a>✅ #22 — Admin: quest-level review rejects the quest and stamps the rejection note on every scene

**Category:** Integration

**What it does:** rejects the quest and stamps the rejection note on every scene

**Test location:** [`apps/api/tests/admin.test.ts`](apps/api/tests/admin.test.ts)

### <a id="test-23"></a>✅ #23 — Admin: quest-level review returns 404 when reviewing a non-existent quest

**Category:** Integration

**What it does:** returns 404 when reviewing a non-existent quest

**Test location:** [`apps/api/tests/admin.test.ts`](apps/api/tests/admin.test.ts)

### <a id="test-24"></a>✅ #24 — Admin: quest-level review rejects a payload with an invalid status enum

**Category:** Integration

**What it does:** rejects a payload with an invalid status enum

**Test location:** [`apps/api/tests/admin.test.ts`](apps/api/tests/admin.test.ts)

### <a id="test-25"></a>✅ #25 — Admin: scene order in submissions feed returns scenes in addScene insertion order, with sceneIndex 0..N matching it

**Category:** Integration

**What it does:** returns scenes in addScene insertion order, with sceneIndex 0..N matching it

**Test location:** [`apps/api/tests/admin.test.ts`](apps/api/tests/admin.test.ts)

### <a id="test-26"></a>✅ #26 — API Application Health Check should return health status

**Category:** Integration

**What it does:** should return health status

**Test location:** [`apps/api/tests/app.test.ts`](apps/api/tests/app.test.ts)

### <a id="test-27"></a>✅ #27 — API Application App Bootstrap should have CORS configured

**Category:** Integration

**What it does:** should have CORS configured

**Test location:** [`apps/api/tests/app.test.ts`](apps/api/tests/app.test.ts)

### <a id="test-28"></a>✅ #28 — API Application App Bootstrap should have JWT configured

**Category:** Integration

**What it does:** should have JWT configured

**Test location:** [`apps/api/tests/app.test.ts`](apps/api/tests/app.test.ts)

### <a id="test-29"></a>✅ #29 — API Application App Bootstrap should have cookie support

**Category:** Integration

**What it does:** should have cookie support

**Test location:** [`apps/api/tests/app.test.ts`](apps/api/tests/app.test.ts)

### <a id="test-30"></a>✅ #30 — API Application Users Routes should have users routes registered

**Category:** Integration

**What it does:** should have users routes registered

**Test location:** [`apps/api/tests/app.test.ts`](apps/api/tests/app.test.ts)

### <a id="test-31"></a>✅ #31 — Auth: protected routes rejects /api/users/me with no token

**Category:** Integration

**What it does:** rejects /api/users/me with no token

**Test location:** [`apps/api/tests/auth.test.ts`](apps/api/tests/auth.test.ts)

### <a id="test-32"></a>✅ #32 — Auth: protected routes returns the user for /api/users/me with a valid token

**Category:** Integration

**What it does:** returns the user for /api/users/me with a valid token

**Test location:** [`apps/api/tests/auth.test.ts`](apps/api/tests/auth.test.ts)

### <a id="test-33"></a>✅ #33 — Auth: protected routes rejects /api/users/me with a malformed token

**Category:** Integration

**What it does:** rejects /api/users/me with a malformed token

**Test location:** [`apps/api/tests/auth.test.ts`](apps/api/tests/auth.test.ts)

### <a id="test-34"></a>✅ #34 — Quest publish + submit-for-review refuses to publish a quest with no waypoints

**Category:** Integration

**What it does:** refuses to publish a quest with no waypoints

**Test location:** [`apps/api/tests/quest-publish.test.ts`](apps/api/tests/quest-publish.test.ts)

### <a id="test-35"></a>✅ #35 — Quest publish + submit-for-review refuses to publish a quest with no scenes

**Category:** Integration

**What it does:** refuses to publish a quest with no scenes

**Test location:** [`apps/api/tests/quest-publish.test.ts`](apps/api/tests/quest-publish.test.ts)

### <a id="test-36"></a>✅ #36 — Quest publish + submit-for-review publishes a quest that has a waypoint and a scene

**Category:** Integration

**What it does:** publishes a quest that has a waypoint and a scene

**Test location:** [`apps/api/tests/quest-publish.test.ts`](apps/api/tests/quest-publish.test.ts)

### <a id="test-37"></a>✅ #37 — Quest publish + submit-for-review refuses submit-for-review when any scene is missing media

**Category:** Integration

**What it does:** refuses submit-for-review when any scene is missing media

**Test location:** [`apps/api/tests/quest-publish.test.ts`](apps/api/tests/quest-publish.test.ts)

### <a id="test-38"></a>✅ #38 — Quest publish + submit-for-review marks the quest pending when all scenes have media

**Category:** Integration

**What it does:** marks the quest pending when all scenes have media

**Test location:** [`apps/api/tests/quest-publish.test.ts`](apps/api/tests/quest-publish.test.ts)

### <a id="test-39"></a>✅ #39 — Edit-published-quest re-review trigger flags submissionStatus=needs_re_review when a published quest title changes

**Category:** Integration

**What it does:** flags submissionStatus=needs_re_review when a published quest title changes

**Test location:** [`apps/api/tests/quest-publish.test.ts`](apps/api/tests/quest-publish.test.ts)

### <a id="test-40"></a>✅ #40 — Edit-published-quest re-review trigger does not flag re-review when the changed field is non-content (e.g. coordinates)

**Category:** Integration

**What it does:** does not flag re-review when the changed field is non-content (e.g. coordinates)

**Test location:** [`apps/api/tests/quest-publish.test.ts`](apps/api/tests/quest-publish.test.ts)

### <a id="test-41"></a>✅ #41 — Edit-published-quest re-review trigger does not re-flag a quest already pending review

**Category:** Integration

**What it does:** does not re-flag a quest already pending review

**Test location:** [`apps/api/tests/quest-publish.test.ts`](apps/api/tests/quest-publish.test.ts)

### <a id="test-42"></a>✅ #42 — Quests CRUD creates a quest with the authenticated user as author

**Category:** Integration

**What it does:** creates a quest with the authenticated user as author

**Test location:** [`apps/api/tests/quests.test.ts`](apps/api/tests/quests.test.ts)

### <a id="test-43"></a>✅ #43 — Quests CRUD rejects quest creation without auth

**Category:** Integration

**What it does:** rejects quest creation without auth

**Test location:** [`apps/api/tests/quests.test.ts`](apps/api/tests/quests.test.ts)

### <a id="test-44"></a>✅ #44 — Quests CRUD rejects quest creation with empty title

**Category:** Integration

**What it does:** rejects quest creation with empty title

**Test location:** [`apps/api/tests/quests.test.ts`](apps/api/tests/quests.test.ts)

### <a id="test-45"></a>✅ #45 — Quests CRUD returns only my quests from /api/quests/my

**Category:** Integration

**What it does:** returns only my quests from /api/quests/my

**Test location:** [`apps/api/tests/quests.test.ts`](apps/api/tests/quests.test.ts)

### <a id="test-46"></a>✅ #46 — Quests CRUD updates a quest the user owns

**Category:** Integration

**What it does:** updates a quest the user owns

**Test location:** [`apps/api/tests/quests.test.ts`](apps/api/tests/quests.test.ts)

### <a id="test-47"></a>✅ #47 — Quests CRUD refuses to update a quest the user does not own

**Category:** Integration

**What it does:** refuses to update a quest the user does not own

**Test location:** [`apps/api/tests/quests.test.ts`](apps/api/tests/quests.test.ts)

### <a id="test-48"></a>✅ #48 — Quests CRUD deletes a quest the user owns

**Category:** Integration

**What it does:** deletes a quest the user owns

**Test location:** [`apps/api/tests/quests.test.ts`](apps/api/tests/quests.test.ts)

### <a id="test-49"></a>✅ #49 — Quests CRUD accepts ageRating E10+ (the value the creator-station UI offers)

**Category:** Integration

**What it does:** accepts ageRating E10+ (the value the creator-station UI offers)

**Test location:** [`apps/api/tests/quests.test.ts`](apps/api/tests/quests.test.ts)

### <a id="test-50"></a>✅ #50 — Quests CRUD accepts mediaType=both for mixed-format quests

**Category:** Integration

**What it does:** accepts mediaType=both for mixed-format quests

**Test location:** [`apps/api/tests/quests.test.ts`](apps/api/tests/quests.test.ts)

### <a id="test-51"></a>✅ #51 — Quests CRUD rejects an unknown mediaType value

**Category:** Integration

**What it does:** rejects an unknown mediaType value

**Test location:** [`apps/api/tests/quests.test.ts`](apps/api/tests/quests.test.ts)

### <a id="test-52"></a>✅ #52 — Scene CRUD creates a scene under a quest the user owns

**Category:** Integration

**What it does:** creates a scene under a quest the user owns

**Test location:** [`apps/api/tests/scenes.test.ts`](apps/api/tests/scenes.test.ts)

### <a id="test-53"></a>✅ #53 — Scene CRUD refuses to create a scene under a quest the user does not own

**Category:** Integration

**What it does:** refuses to create a scene under a quest the user does not own

**Test location:** [`apps/api/tests/scenes.test.ts`](apps/api/tests/scenes.test.ts)

### <a id="test-54"></a>✅ #54 — Scene CRUD updates and deletes a scene

**Category:** Integration

**What it does:** updates and deletes a scene

**Test location:** [`apps/api/tests/scenes.test.ts`](apps/api/tests/scenes.test.ts)

### <a id="test-55"></a>✅ #55 — Scene media upload rejects upload to an unknown sceneId with 404 (not "Load failed")

**Category:** Integration

**What it does:** rejects upload to an unknown sceneId with 404 (not "Load failed")

**Test location:** [`apps/api/tests/scenes.test.ts`](apps/api/tests/scenes.test.ts)

### <a id="test-56"></a>✅ #56 — Scene media upload rejects upload of a disallowed mime type

**Category:** Integration

**What it does:** rejects upload of a disallowed mime type

**Test location:** [`apps/api/tests/scenes.test.ts`](apps/api/tests/scenes.test.ts)

### <a id="test-57"></a>✅ #57 — Scene media upload accepts an audio upload and attaches mediaUrl to the scene

**Category:** Integration

**What it does:** accepts an audio upload and attaches mediaUrl to the scene

**Test location:** [`apps/api/tests/scenes.test.ts`](apps/api/tests/scenes.test.ts)

### <a id="test-58"></a>✅ #58 — Scouted Waypoints — edit flow PATCHes name + notes and persists across reads

**Category:** Integration

**What it does:** PATCHes name + notes and persists across reads

**Test location:** [`apps/api/tests/scouted-waypoints.test.ts`](apps/api/tests/scouted-waypoints.test.ts)

### <a id="test-59"></a>✅ #59 — Scouted Waypoints — edit flow PATCHes lat/lng (move-pin flow)

**Category:** Integration

**What it does:** PATCHes lat/lng (move-pin flow)

**Test location:** [`apps/api/tests/scouted-waypoints.test.ts`](apps/api/tests/scouted-waypoints.test.ts)

### <a id="test-60"></a>✅ #60 — Scouted Waypoints — edit flow rejects PATCH from a different user with 404

**Category:** Integration

**What it does:** rejects PATCH from a different user with 404

**Test location:** [`apps/api/tests/scouted-waypoints.test.ts`](apps/api/tests/scouted-waypoints.test.ts)

### <a id="test-61"></a>✅ #61 — Scouted Waypoints — edit flow rejects an empty name (validation)

**Category:** Integration

**What it does:** rejects an empty name (validation)

**Test location:** [`apps/api/tests/scouted-waypoints.test.ts`](apps/api/tests/scouted-waypoints.test.ts)

### <a id="test-62"></a>✅ #62 — Scouted Waypoints — edit flow rejects unauthenticated PATCH with 401

**Category:** Integration

**What it does:** rejects unauthenticated PATCH with 401

**Test location:** [`apps/api/tests/scouted-waypoints.test.ts`](apps/api/tests/scouted-waypoints.test.ts)

### <a id="test-63"></a>✅ #63 — Waypoint CRUD creates a waypoint under a quest the user owns

**Category:** Integration

**What it does:** creates a waypoint under a quest the user owns

**Test location:** [`apps/api/tests/waypoints.test.ts`](apps/api/tests/waypoints.test.ts)

### <a id="test-64"></a>✅ #64 — Waypoint CRUD rejects creating a waypoint under another user's quest

**Category:** Integration

**What it does:** rejects creating a waypoint under another user's quest

**Test location:** [`apps/api/tests/waypoints.test.ts`](apps/api/tests/waypoints.test.ts)

### <a id="test-65"></a>✅ #65 — Waypoint CRUD PATCHes a waypoint name and the new name persists across reads

**Category:** Integration

**What it does:** PATCHes a waypoint name and the new name persists across reads

**Test location:** [`apps/api/tests/waypoints.test.ts`](apps/api/tests/waypoints.test.ts)

### <a id="test-66"></a>✅ #66 — Waypoint CRUD rejects PATCH from a non-owner with 404

**Category:** Integration

**What it does:** rejects PATCH from a non-owner with 404

**Test location:** [`apps/api/tests/waypoints.test.ts`](apps/api/tests/waypoints.test.ts)

### <a id="test-67"></a>✅ #67 — Waypoint CRUD deletes a waypoint the user owns

**Category:** Integration

**What it does:** deletes a waypoint the user owns

**Test location:** [`apps/api/tests/waypoints.test.ts`](apps/api/tests/waypoints.test.ts)

