# Testing

Auto-generated by `/testing` (see `scripts/update-testing-doc.mjs`). Do not edit by hand.

_Last run: 2026-05-02T20:53:07.399Z_

## Summary

- ✅ **37 passing**
- ❌ **0 failing**
- **37 total**

### ✅ Passing tests

- ✅ [#1](#test-1) — Admin: submissions feed lists quests with a submissionStatus
- ✅ [#2](#test-2) — Admin: submissions feed omits quests with no submissionStatus (pure drafts)
- ✅ [#3](#test-3) — Admin: submissions feed filters by submissionStatus query param
- ✅ [#4](#test-4) — Admin: quest-level review approves the quest and cascades approved status + notes to every scene
- ✅ [#5](#test-5) — Admin: quest-level review rejects the quest and stamps the rejection note on every scene
- ✅ [#6](#test-6) — Admin: quest-level review returns 404 when reviewing a non-existent quest
- ✅ [#7](#test-7) — Admin: quest-level review rejects a payload with an invalid status enum
- ✅ [#8](#test-8) — API Application Health Check should return health status
- ✅ [#9](#test-9) — API Application App Bootstrap should have CORS configured
- ✅ [#10](#test-10) — API Application App Bootstrap should have JWT configured
- ✅ [#11](#test-11) — API Application App Bootstrap should have cookie support
- ✅ [#12](#test-12) — API Application Users Routes should have users routes registered
- ✅ [#13](#test-13) — Auth: protected routes rejects /api/users/me with no token
- ✅ [#14](#test-14) — Auth: protected routes returns the user for /api/users/me with a valid token
- ✅ [#15](#test-15) — Auth: protected routes rejects /api/users/me with a malformed token
- ✅ [#16](#test-16) — Quest publish + submit-for-review refuses to publish a quest with no waypoints
- ✅ [#17](#test-17) — Quest publish + submit-for-review refuses to publish a quest with no scenes
- ✅ [#18](#test-18) — Quest publish + submit-for-review publishes a quest that has a waypoint and a scene
- ✅ [#19](#test-19) — Quest publish + submit-for-review refuses submit-for-review when any scene is missing media
- ✅ [#20](#test-20) — Quest publish + submit-for-review marks the quest pending when all scenes have media
- ✅ [#21](#test-21) — Edit-published-quest re-review trigger flags submissionStatus=needs_re_review when a published quest title changes
- ✅ [#22](#test-22) — Edit-published-quest re-review trigger does not flag re-review when the changed field is non-content (e.g. coordinates)
- ✅ [#23](#test-23) — Edit-published-quest re-review trigger does not re-flag a quest already pending review
- ✅ [#24](#test-24) — Quests CRUD creates a quest with the authenticated user as author
- ✅ [#25](#test-25) — Quests CRUD rejects quest creation without auth
- ✅ [#26](#test-26) — Quests CRUD rejects quest creation with empty title
- ✅ [#27](#test-27) — Quests CRUD returns only my quests from /api/quests/my
- ✅ [#28](#test-28) — Quests CRUD updates a quest the user owns
- ✅ [#29](#test-29) — Quests CRUD refuses to update a quest the user does not own
- ✅ [#30](#test-30) — Quests CRUD deletes a quest the user owns
- ✅ [#31](#test-31) — Quests CRUD accepts ageRating E10+ (the value the creator-station UI offers)
- ✅ [#32](#test-32) — Scene CRUD creates a scene under a quest the user owns
- ✅ [#33](#test-33) — Scene CRUD refuses to create a scene under a quest the user does not own
- ✅ [#34](#test-34) — Scene CRUD updates and deletes a scene
- ✅ [#35](#test-35) — Scene media upload rejects upload to an unknown sceneId with 404 (not "Load failed")
- ✅ [#36](#test-36) — Scene media upload rejects upload of a disallowed mime type
- ✅ [#37](#test-37) — Scene media upload accepts an audio upload and attaches mediaUrl to the scene

---

## Tests

### <a id="test-1"></a>✅ #1 — Admin: submissions feed lists quests with a submissionStatus

**Category:** Integration

**What it does:** lists quests with a submissionStatus

**Test location:** [`apps/api/tests/admin.test.ts`](apps/api/tests/admin.test.ts)

### <a id="test-2"></a>✅ #2 — Admin: submissions feed omits quests with no submissionStatus (pure drafts)

**Category:** Integration

**What it does:** omits quests with no submissionStatus (pure drafts)

**Test location:** [`apps/api/tests/admin.test.ts`](apps/api/tests/admin.test.ts)

### <a id="test-3"></a>✅ #3 — Admin: submissions feed filters by submissionStatus query param

**Category:** Integration

**What it does:** filters by submissionStatus query param

**Test location:** [`apps/api/tests/admin.test.ts`](apps/api/tests/admin.test.ts)

### <a id="test-4"></a>✅ #4 — Admin: quest-level review approves the quest and cascades approved status + notes to every scene

**Category:** Integration

**What it does:** approves the quest and cascades approved status + notes to every scene

**Test location:** [`apps/api/tests/admin.test.ts`](apps/api/tests/admin.test.ts)

### <a id="test-5"></a>✅ #5 — Admin: quest-level review rejects the quest and stamps the rejection note on every scene

**Category:** Integration

**What it does:** rejects the quest and stamps the rejection note on every scene

**Test location:** [`apps/api/tests/admin.test.ts`](apps/api/tests/admin.test.ts)

### <a id="test-6"></a>✅ #6 — Admin: quest-level review returns 404 when reviewing a non-existent quest

**Category:** Integration

**What it does:** returns 404 when reviewing a non-existent quest

**Test location:** [`apps/api/tests/admin.test.ts`](apps/api/tests/admin.test.ts)

### <a id="test-7"></a>✅ #7 — Admin: quest-level review rejects a payload with an invalid status enum

**Category:** Integration

**What it does:** rejects a payload with an invalid status enum

**Test location:** [`apps/api/tests/admin.test.ts`](apps/api/tests/admin.test.ts)

### <a id="test-8"></a>✅ #8 — API Application Health Check should return health status

**Category:** Integration

**What it does:** should return health status

**Test location:** [`apps/api/tests/app.test.ts`](apps/api/tests/app.test.ts)

### <a id="test-9"></a>✅ #9 — API Application App Bootstrap should have CORS configured

**Category:** Integration

**What it does:** should have CORS configured

**Test location:** [`apps/api/tests/app.test.ts`](apps/api/tests/app.test.ts)

### <a id="test-10"></a>✅ #10 — API Application App Bootstrap should have JWT configured

**Category:** Integration

**What it does:** should have JWT configured

**Test location:** [`apps/api/tests/app.test.ts`](apps/api/tests/app.test.ts)

### <a id="test-11"></a>✅ #11 — API Application App Bootstrap should have cookie support

**Category:** Integration

**What it does:** should have cookie support

**Test location:** [`apps/api/tests/app.test.ts`](apps/api/tests/app.test.ts)

### <a id="test-12"></a>✅ #12 — API Application Users Routes should have users routes registered

**Category:** Integration

**What it does:** should have users routes registered

**Test location:** [`apps/api/tests/app.test.ts`](apps/api/tests/app.test.ts)

### <a id="test-13"></a>✅ #13 — Auth: protected routes rejects /api/users/me with no token

**Category:** Integration

**What it does:** rejects /api/users/me with no token

**Test location:** [`apps/api/tests/auth.test.ts`](apps/api/tests/auth.test.ts)

### <a id="test-14"></a>✅ #14 — Auth: protected routes returns the user for /api/users/me with a valid token

**Category:** Integration

**What it does:** returns the user for /api/users/me with a valid token

**Test location:** [`apps/api/tests/auth.test.ts`](apps/api/tests/auth.test.ts)

### <a id="test-15"></a>✅ #15 — Auth: protected routes rejects /api/users/me with a malformed token

**Category:** Integration

**What it does:** rejects /api/users/me with a malformed token

**Test location:** [`apps/api/tests/auth.test.ts`](apps/api/tests/auth.test.ts)

### <a id="test-16"></a>✅ #16 — Quest publish + submit-for-review refuses to publish a quest with no waypoints

**Category:** Integration

**What it does:** refuses to publish a quest with no waypoints

**Test location:** [`apps/api/tests/quest-publish.test.ts`](apps/api/tests/quest-publish.test.ts)

### <a id="test-17"></a>✅ #17 — Quest publish + submit-for-review refuses to publish a quest with no scenes

**Category:** Integration

**What it does:** refuses to publish a quest with no scenes

**Test location:** [`apps/api/tests/quest-publish.test.ts`](apps/api/tests/quest-publish.test.ts)

### <a id="test-18"></a>✅ #18 — Quest publish + submit-for-review publishes a quest that has a waypoint and a scene

**Category:** Integration

**What it does:** publishes a quest that has a waypoint and a scene

**Test location:** [`apps/api/tests/quest-publish.test.ts`](apps/api/tests/quest-publish.test.ts)

### <a id="test-19"></a>✅ #19 — Quest publish + submit-for-review refuses submit-for-review when any scene is missing media

**Category:** Integration

**What it does:** refuses submit-for-review when any scene is missing media

**Test location:** [`apps/api/tests/quest-publish.test.ts`](apps/api/tests/quest-publish.test.ts)

### <a id="test-20"></a>✅ #20 — Quest publish + submit-for-review marks the quest pending when all scenes have media

**Category:** Integration

**What it does:** marks the quest pending when all scenes have media

**Test location:** [`apps/api/tests/quest-publish.test.ts`](apps/api/tests/quest-publish.test.ts)

### <a id="test-21"></a>✅ #21 — Edit-published-quest re-review trigger flags submissionStatus=needs_re_review when a published quest title changes

**Category:** Integration

**What it does:** flags submissionStatus=needs_re_review when a published quest title changes

**Test location:** [`apps/api/tests/quest-publish.test.ts`](apps/api/tests/quest-publish.test.ts)

### <a id="test-22"></a>✅ #22 — Edit-published-quest re-review trigger does not flag re-review when the changed field is non-content (e.g. coordinates)

**Category:** Integration

**What it does:** does not flag re-review when the changed field is non-content (e.g. coordinates)

**Test location:** [`apps/api/tests/quest-publish.test.ts`](apps/api/tests/quest-publish.test.ts)

### <a id="test-23"></a>✅ #23 — Edit-published-quest re-review trigger does not re-flag a quest already pending review

**Category:** Integration

**What it does:** does not re-flag a quest already pending review

**Test location:** [`apps/api/tests/quest-publish.test.ts`](apps/api/tests/quest-publish.test.ts)

### <a id="test-24"></a>✅ #24 — Quests CRUD creates a quest with the authenticated user as author

**Category:** Integration

**What it does:** creates a quest with the authenticated user as author

**Test location:** [`apps/api/tests/quests.test.ts`](apps/api/tests/quests.test.ts)

### <a id="test-25"></a>✅ #25 — Quests CRUD rejects quest creation without auth

**Category:** Integration

**What it does:** rejects quest creation without auth

**Test location:** [`apps/api/tests/quests.test.ts`](apps/api/tests/quests.test.ts)

### <a id="test-26"></a>✅ #26 — Quests CRUD rejects quest creation with empty title

**Category:** Integration

**What it does:** rejects quest creation with empty title

**Test location:** [`apps/api/tests/quests.test.ts`](apps/api/tests/quests.test.ts)

### <a id="test-27"></a>✅ #27 — Quests CRUD returns only my quests from /api/quests/my

**Category:** Integration

**What it does:** returns only my quests from /api/quests/my

**Test location:** [`apps/api/tests/quests.test.ts`](apps/api/tests/quests.test.ts)

### <a id="test-28"></a>✅ #28 — Quests CRUD updates a quest the user owns

**Category:** Integration

**What it does:** updates a quest the user owns

**Test location:** [`apps/api/tests/quests.test.ts`](apps/api/tests/quests.test.ts)

### <a id="test-29"></a>✅ #29 — Quests CRUD refuses to update a quest the user does not own

**Category:** Integration

**What it does:** refuses to update a quest the user does not own

**Test location:** [`apps/api/tests/quests.test.ts`](apps/api/tests/quests.test.ts)

### <a id="test-30"></a>✅ #30 — Quests CRUD deletes a quest the user owns

**Category:** Integration

**What it does:** deletes a quest the user owns

**Test location:** [`apps/api/tests/quests.test.ts`](apps/api/tests/quests.test.ts)

### <a id="test-31"></a>✅ #31 — Quests CRUD accepts ageRating E10+ (the value the creator-station UI offers)

**Category:** Integration

**What it does:** accepts ageRating E10+ (the value the creator-station UI offers)

**Test location:** [`apps/api/tests/quests.test.ts`](apps/api/tests/quests.test.ts)

### <a id="test-32"></a>✅ #32 — Scene CRUD creates a scene under a quest the user owns

**Category:** Integration

**What it does:** creates a scene under a quest the user owns

**Test location:** [`apps/api/tests/scenes.test.ts`](apps/api/tests/scenes.test.ts)

### <a id="test-33"></a>✅ #33 — Scene CRUD refuses to create a scene under a quest the user does not own

**Category:** Integration

**What it does:** refuses to create a scene under a quest the user does not own

**Test location:** [`apps/api/tests/scenes.test.ts`](apps/api/tests/scenes.test.ts)

### <a id="test-34"></a>✅ #34 — Scene CRUD updates and deletes a scene

**Category:** Integration

**What it does:** updates and deletes a scene

**Test location:** [`apps/api/tests/scenes.test.ts`](apps/api/tests/scenes.test.ts)

### <a id="test-35"></a>✅ #35 — Scene media upload rejects upload to an unknown sceneId with 404 (not "Load failed")

**Category:** Integration

**What it does:** rejects upload to an unknown sceneId with 404 (not "Load failed")

**Test location:** [`apps/api/tests/scenes.test.ts`](apps/api/tests/scenes.test.ts)

### <a id="test-36"></a>✅ #36 — Scene media upload rejects upload of a disallowed mime type

**Category:** Integration

**What it does:** rejects upload of a disallowed mime type

**Test location:** [`apps/api/tests/scenes.test.ts`](apps/api/tests/scenes.test.ts)

### <a id="test-37"></a>✅ #37 — Scene media upload accepts an audio upload and attaches mediaUrl to the scene

**Category:** Integration

**What it does:** accepts an audio upload and attaches mediaUrl to the scene

**Test location:** [`apps/api/tests/scenes.test.ts`](apps/api/tests/scenes.test.ts)

